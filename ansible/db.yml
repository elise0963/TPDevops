- hosts: db
  become: yes
  tasks:
    - name: Install mariadb
      apt:
        name: mariadb-server
        state: present
        update_cache: yes

    - name: Start mariadb
      service:
        name: mariadb
        state: started
        enabled: yes
